export class Filter {
    filterKey: string;
    filterValue: string;

    constructor(filterKey: string, filterValue: string) {
        this.filterKey = filterKey;
        this.filterValue = filterValue;
    }
}